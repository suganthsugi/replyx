/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * ReplyX API
 * Staff and customer API of the ReplyX multi-tenant helpdesk. The tenant always comes from the host. Authentication uses the `rx_session` cookie, and state-changing requests must send an `X-CSRF-Token` header matching the `rx_csrf` cookie. Lists are cursor-paginated and wrapped as `{ "items": [...], "nextCursor": "..." | null }`. Creating POSTs accept an optional `Idempotency-Key` header (kept 24 h).

 * OpenAPI spec version: 0.1.0
 */
import type { MeAvatarUrl } from './meAvatarUrl';
import type { MeKind } from './meKind';
import type { Availability } from './availability';
import type { MeRolesItem } from './meRolesItem';
import type { MeGroupAccessItem } from './meGroupAccessItem';

export interface Me {
  id: string;
  email: string;
  name: string;
  avatarUrl?: MeAvatarUrl;
  kind: MeKind;
  availability?: Availability;
  roles: MeRolesItem[];
  permissions: string[];
  /** Effective (union) access; groupId null = Ungrouped */
  groupAccess: MeGroupAccessItem[];
  /** Changes whenever access changes; clients refetch /me when it moves */
  accessVersion: number;
}
